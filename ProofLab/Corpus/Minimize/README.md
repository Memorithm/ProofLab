# PL-1.2 assumption-minimization fixtures

Known proved statements with intentionally redundant (or load-bearing) binders.
Used to measure automatic assumption removal followed by Lean re-verification.

Acceptance modules in this directory are imported by the `ProofLab` Lake library.
Rejection controls live under `ProofLab/Corpus/Fixtures/` and are **not** Lake targets.

No mathematical novelty is claimed.
